# Nginx Configuration

This directory contains static file server and reverse proxy parameters.

## Hackathon TODO
- Configure reverse proxy mapping to route requests between frontend client and API endpoints.
- Configure gzip compressions and headers for static asset distributions.
